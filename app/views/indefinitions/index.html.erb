<p id="notice"><%= notice %></p>

<h1>Have fun!</h1>

<p><%= link_to 'update', '/update_indefs' %></p>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Auteur</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @indefinitions.each do |indefinition| %>
      <tr>
        <td><%= indefinition.name %></td>
        <td><%= indefinition.description %></td>
        <td><%= indefinition.auteur %></td>
        <td><%= link_to 'Show', indefinition %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
